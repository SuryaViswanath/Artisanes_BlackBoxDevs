{
  "name": "artisan-marketplace",
  "version": "1.0.0",
  "description": "AI-powered marketplace for local artisans - monorepo (frontend + backend)",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "dev": "cd frontend && npm run dev",
    "dev:backend": "cd backend && node server.js",
    "backend": "cd backend && node server.js",
    "backend:restart": "lsof -ti:3001 | xargs kill -9 2>/dev/null; npm run backend",
    "build": "cd frontend && npm run build",
    "build:all": "cd frontend && npm run build"
  },
  "keywords": ["artisan", "marketplace", "ai", "aws", "bedrock"],
  "author": "",
  "license": "MIT"
}
